name: "sneaker_intel"
version: "1.0.0"
config-version: 2

profile: "sneaker_intel"

model-paths: ["models"]
test-paths: ["tests"]
target-path: "target"
clean-targets: ["target", "dbt_packages", "logs"]

models:
  sneaker_intel:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
