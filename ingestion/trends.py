"""trends source client. Implemented in Phase 1.

Pattern: a dataclass for the record shape + a client class whose public
method is a generator yielding those records, with private HTTP/parse helpers.
"""
